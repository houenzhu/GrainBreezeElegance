<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhe.grain.mapper.commodity.CommoditySpuInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zhe.grain.domain.commodity.CommoditySpuInfo">
        <id column="id" property="id" />
        <result column="spu_name" property="spuName" />
        <result column="spu_description" property="spuDescription" />
        <result column="catalog_id" property="catalogId" />
        <result column="brand_id" property="brandId" />
        <result column="weight" property="weight" />
        <result column="publish_status" property="publishStatus" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, spu_name, spu_description, catalog_id, brand_id, weight, publish_status, create_time, update_time
    </sql>
    <update id="updatePublishStatus">
        UPDATE `commodity_spu_info` SET publish_status = #{publishStatus}, update_time = #{time}
        <where>
            id = #{spuId}
        </where>
    </update>

</mapper>
